// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Put your stable config options here.
  // Example:
  // reactStrictMode: true,
  // experimental flags should go here ONLY if they exist in the official Next.js 16 type.
};

export default nextConfig;
